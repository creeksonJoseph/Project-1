{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.8",
    "tailwindcss": "^4.1.8"
  }
}
{
  "name": "json-api",
  "version": "1.0.0",
  "description": "Deployed JSON server on Cyclic",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch data.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
